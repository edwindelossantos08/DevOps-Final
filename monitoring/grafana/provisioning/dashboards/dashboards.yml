# Carga automatica de los dashboards versionados en el repositorio.

apiVersion: 1

providers:
  - name: TaskFlow
    orgId: 1
    folder: TaskFlow
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
